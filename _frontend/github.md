repo: juliangarcianagyabi/02_aidevtools
branch: main

## Last sync

date: 2026-09-11T00:00:00Z

### Updated in this project

- Built the Simple CRM MVP from the written spec — the repository is empty (no commits), so nothing was imported.
- Services layer (`crm-service.js`) and its mock implementation added.
- Test suite (`crm-tests.js`) added, surfaced as a Tests route in the app.

## Screen map

| Screen | Repo files |
| --- | --- |
| Contacts (list, search, tag filters, detail, activities, deals) | — (built from spec) |
| Deals (pipeline board) | — (built from spec) |
| Tests | — (built from spec) |
